<Window x:Class="ChatAppRealTime.Menu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ChatAppRealTime" Height="1050" Width="1400"
                                      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d" 
            >
    <Window.Background>
        <SolidColorBrush  Color="Black"/>
    </Window.Background>
    <StackPanel Margin="0,0,0,24">
        <ui:NavigationView
            Height="800"
                MinHeight="300"
                Margin="0" 
                IsBackButtonVisible="Collapsed"
                IsPaneToggleVisible="False"
                PaneDisplayMode="LeftFluent">
            <ui:NavigationView.MenuItems>

                <ui:NavigationViewItem
                        Content="Chat"
                        Icon="{ui:SymbolIcon Chat12}"
                        TargetPageType="{x:Type local:ChatRoom}" />
                <ui:NavigationViewItem
                        Content="Chat Ai"
                        Icon="{ui:SymbolIcon ChatBubblesQuestion20}"
                        TargetPageType="{x:Type local:ChatAi}" />
            </ui:NavigationView.MenuItems>
            <ui:NavigationView.FooterMenuItems>
                <ui:NavigationViewItem
                     Content="settings"
 Icon="{ui:SymbolIcon settings24}"
 TargetPageType="{x:Type local:ChatAi}" />
                       
            </ui:NavigationView.FooterMenuItems>
            <ui:NavigationView.Header>
                <Border
                        Margin="8"
                        Background="AliceBlue"
                    
                        CornerRadius="4">
                    <TextBlock
                            Margin="24"
                            VerticalAlignment="Center"
                            FontWeight="Medium"
                            Foreground="Black"
                            Text="Home" />
                </Border>
            </ui:NavigationView.Header>




        </ui:NavigationView>
    </StackPanel>
</Window>
